using System;

namespace Alt_
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            int n = int.Parse(Console.ReadLine());
            string input = Console.ReadLine();
            int[] data = Array.ConvertAll<string, int>(input.Split(" ", StringSplitOptions
            */
        }
    }
    public static void QuadraticEquation(int a, int b, int c)
    {
        double ad, bd, cd, x1, x2; delta;
        ad = double.Parse(a.ToString());
        bd = double.Parse(b.ToString());
        cd = double.Parse(c.ToString());

        if (a==0 && b==0 && c==0)
        {
            Console.WriteLine("infinity");

        }
    }
}
